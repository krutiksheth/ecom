import {Typography} from "@mui/material";

const CheckoutPage = () => {
    return (
        <Typography variant="h3">
            Only authorized users can use the checkout page.
        </Typography>
    );
};

export default CheckoutPage;
